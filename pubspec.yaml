name: api_open_builder
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.3.4

# Add regular dependencies here.
dependencies:
  collection: ^1.18.0
  json_schema: ^5.1.7
  dart_mappable: ^4.2.2
  # path: ^1.8.0

dev_dependencies:
  build_runner: ^2.4.10
  dart_mappable_builder: ^4.2.3
  lints: ^3.0.0
  test: ^1.24.0
